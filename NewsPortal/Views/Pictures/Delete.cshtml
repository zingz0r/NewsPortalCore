@model NewsPortal.Data.Entity.Picture

@{
    ViewData["Title"] = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Picture</h4>
    <hr />
    <dl class="dl-horizontal">
        @*<dt>
            @Html.DisplayNameFor(model => model.LargeImageData)
        </dt>
        <dd>
            <img src="@Url.Action("GetImage", "Manager", new { id = Model.Id, isLarge = true })" class="img-thumbnail">
        </dd>*@
        <dt>
            @Html.DisplayNameFor(model => model.SmallImageData)
        </dt>
        <dd>
            <img src="@Url.Action("GetImage", "Manager", new { id = Model.Id, isLarge = false })" class="img-thumbnail">
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Article)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Article.Summary)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-default" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
